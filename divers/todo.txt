-TODO dans testMySql
-BDD : prévenir changements + changer sur le serveur (vérifier trigger OK)

-CAHIER DES CHARGES (rappel : tarif à zéro autorisé + date du vol à partir du lendemain + contraintes sur les modifs d'aéroports / vols)
-DSPP
-avoir l'exécutable et modifier au préalable l'adresse de la BDD

-commentaires
-relire + tester l'appli

Rqs à intégrer dans le rapport :
- fuseau horaire du pays de départ et non de destination
- problème de conception avec la ville utilisée comme "clé" en PHP (=> 2 villes ne peuvent pas avoir le même nom + un aéroport "utilisé" ne peut pas être modifié sous peine de plantage... pas de redondance possible dans les noms de villes)
- le numéro de vol n'a pas d'importance sémantique (le 6 peut partir avant le 2)...
- gestion des exceptions
- on aurait pu faire en sorte de proposer uniquement les employés disponibles à la date
- amélioration : durée calculée automatiquement selon le trajet
- trigger

Notes / Voc :
- Add @SuppressWarnings ("permet de signaler au compilateur que l'on a conscience de réaliser une opération risquée, et qu'il peut donc arrêter de signaler un éventuel problème")
- propager une exception : lors de l'appel, on aura un try / catch
- pour faire un tri autrement que par chaînes de caractères, il autait fallu utiliser (fait planter l'appli si colonne nulle, ex : pas de vol_tmp) :
@Override
public Class<?> getColumnClass(int arg0) {
	return myValues[0][arg0].getClass();
}